[project]
name = "sttool"
version = "0.1.0"
description = "Atomic project launcher for an authorized penetration-testing toolkit"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["playwright>=1.50", "psutil>=5.9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
